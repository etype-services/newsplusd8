<?php

/**
 * Implements hook_page_attachments().
 * css hides system login link.
 *
 * @param array $page
 */
function etype_login_page_attachments(array &$page) {
  $page['#attached']['library'][] = 'etype_login/global-styling';
}